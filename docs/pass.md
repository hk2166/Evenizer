Do not store real credentials in this repository.

Use environment variables or a local password manager for development-only
admin credentials, and rotate any password that was previously committed here.
